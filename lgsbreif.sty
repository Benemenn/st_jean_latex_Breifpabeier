\ProvidesPackage{lgsbreif} % Provides the package name

% Load required packages
\usepackage[a4paper, top=1.5cm, left=27mm, right=15mm, bottom=8cm]{geometry} % Adjust margins
\usepackage{fancyhdr} % For custom headers and footers
\usepackage{graphicx} % For including images
\usepackage{xcolor}   % For defining custom colors
\usepackage{lipsum}   % For dummy text (optional)
\usepackage{hyperref} % For clickable links

% Define custom colors using RGB values converted from 0-255 range
\definecolor{hellblo}{rgb}{0.082, 0.671, 0.627} % Correctly defined RGB values (21, 171, 160)
\definecolor{gro}{rgb}{0.475, 0.475, 0.475}
\definecolor{donkelblo}{rgb}{0.165, 0.012, 0.522}
\definecolor{giel}{rgb}{0.973, 0.965, 0.286}

% Set up hyperlink colors to match the text color and remove the box
\hypersetup{
	colorlinks=true,      % Enable colored links
	linkcolor=gro,        % Color for internal links (e.g., table of contents)
	urlcolor=donkelblo,   % Color for external links (e.g., emails) matching the desired text color
	citecolor=gro,        % Color for citations
	pdfborder={0 0 0}     % Remove the box around links
}

\newcommand{\gruppennumm}{Saint Jean Diddeleng}
\newcommand{\gruppenemail}{lgsdudelange@pt.lu}
\newcommand{\gruppenaddress}{Rue Mont St Jean Dudelange}
\newcommand{\logodatei}{FDL_STJEAN}

% Set up custom header and footer
\pagestyle{fancy} % Use the fancyhdr style
\fancyhf{} % Clear all header and footer fields

% Set header height
\setlength{\headheight}{100pt} % Increase header height to fit the image properly

% Adjust the horizontal offset for the image in the header
\fancyhead[L]{%
	\makebox[0pt][l]{\hspace{-17mm}\includegraphics[width=9cm]{\logodatei}} % Adjust \hspace{-2cm} to move image left
}

% Remove header and footer lines
\renewcommand{\headrulewidth}{0pt} % Remove the line under the header
\renewcommand{\footrulewidth}{0pt} % Remove the line above the footer

% Add contact information above the footer image with custom colors
\fancyfoot[L]{%
	\raisebox{100pt}{ % Adjust this value to fine-tune the contact info position
		\parbox{\textwidth}{%
			\footnotesize % Change text size to suit your needs
			\textbf{\textcolor{gro}{Lëtzebuerger Guiden a Scouten}}\\
			\textbf{\textcolor{hellblo}{Grupp \gruppennumm}}\\
			\textcolor{gro}{\gruppenaddress}\\
			\textbf{\href{mailto:\gruppenemail}{Email: \gruppenemail}}\\ % Clickable email without blue box
		}
	}
}




% Add custom footer image centered in the footer
\fancyfoot[C]{%
	\makebox[0pt][c]{\raisebox{0.45cm}{\includegraphics[width=20.88cm, height=1.96cm]{FOOTER_LGS}}} % Fine-tune footer image position
}

% Add page number on the right side of the footer
\fancyfoot[R]{\raisebox{3cm}{\thepage}} % Adjust page number position if needed

% Adjust the footer height to fit both contact info and image
\setlength{\footskip}{5cm} % Ensure enough space for both text and image
